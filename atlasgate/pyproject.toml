[project]
name = "atlasgate"
version = "0.1.0"
description = "Atlas-aware disclosure + release governance gate for systems-lab"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
labctl = ["labctl"]
dev = ["pytest>=8"]

[project.scripts]
atlasgate = "atlasgate.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["atlasgate*"]

[tool.pytest.ini_options]
testpaths = ["tests"]

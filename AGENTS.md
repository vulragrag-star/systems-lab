# Agent notes

- Research docs in `docs/` are the source of truth for what we pursue.
- Do not expand into abandoned attempts (supply-chain product, ZT edge product, GitOps-in-a-box) without updating ATTEMPTS.md with new prior-art scans.
- Prefer composing upstream CLIs (task, dagger, kind, wasmtime, rclone, litestream) over vendoring forks.
- Use `tursodatabase/libsql`, not upstream sqlite (atlas hard leave / agentic ban).
- Disclosure-policy tools need Assiste/d trailers if AI-assisted.
